import { HasRoleV2Directive } from './has-role-v2.directive';

describe('HasRoleV2Directive', () => {
  it('should create an instance', () => {
    const directive = new HasRoleV2Directive();
    expect(directive).toBeTruthy();
  });
});
